local.settings.json

# Azurite local storage emulator data
AzuriteConfig/
__azurite_db_*.json
__blobstorage__/
__queuestorage__/
.claude/settings.local.json

# Local Azure provisioning scratch (may contain secrets — never commit)
.azure-suffix.txt
.azure-cosmos.json
.azure-appsettings.json

# Env files (VITE_ public config may be committed via .env.production; secrets must not)
.env
.env.*.local

# Test artifacts (the committed sprint report lives in tests/reports/, transient runs are ignored)
test-report.html
test-results/
playwright-report/
playwright/.cache/
coverage/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
