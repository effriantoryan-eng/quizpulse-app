# Production build-time config (Vite, VITE_* only — these are PUBLIC, never put secrets here).
# Populate VITE_B2C_CLIENT_ID after creating the B2C SPA app registration — see
# docs/azure/B2C_SETUP.md. The SPA client id is not a secret.

VITE_B2C_TENANT_NAME=quizpulseb2c
VITE_B2C_POLICY=B2C_1_signupsignin
VITE_B2C_CLIENT_ID=00000000-0000-0000-0000-000000000000
VITE_B2C_API_SCOPE=https://quizpulseb2c.onmicrosoft.com/api/access_as_teacher
